services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: corpshell
      POSTGRES_USER: corpshell
      POSTGRES_PASSWORD: corpshell
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

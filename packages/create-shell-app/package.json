{
  "name": "@corp/create-shell-app",
  "version": "1.0.0",
  "description": "Scaffold and manage Corp shell instances and child apps",
  "bin": {
    "create-shell-app": "./dist/index.js"
  },
  "files": [
    "dist",
    "template"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5",
    "vitest": "^2"
  }
}

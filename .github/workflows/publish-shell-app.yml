name: Publish @s-tkach/shell-app

on:
  push:
    tags:
      - "shell-app/v*.*.*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://npm.pkg.github.com
          scope: "@s-tkach"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build workspace SDK dependency
        run: pnpm --filter @s-tkach/shell-sdk build

      - name: Build (validate source compiles)
        run: pnpm --filter @s-tkach/shell-app build

      - name: Publish
        run: pnpm --filter @s-tkach/shell-app publish --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
